# Agents.md

## 目标

这个仓库用于循序渐进学习数字人技术，不追求一次性做“大而全”的系统，而是按可运行、可理解、可扩展的顺序推进。

当前仓库已经具备一个最小原型：

- `main.py`：提供 FastAPI 页面和 TTS 接口
- `static/index.html`：最简单的页面入口
- `static/main.js`：Canvas 头像绘制 + 音频驱动嘴巴开合
- `output.mp3`：TTS 生成结果

当前阶段可以定义为：

`阶段 0：静态头像 + 文本转语音 + 简单嘴型联动`

---

## 学习路线

后续默认按下面顺序学习和实现。

### 阶段 1：跑通最小数字人链路

目标：

- 理解前端、后端、音频、头像之间的基本调用关系
- 能独立修改文本、播放语音、观察嘴型变化

本阶段重点：

- FastAPI 基础
- `edge-tts` 的使用
- 浏览器音频播放
- Canvas 基础绘制
- 用音量而不是音素控制嘴型

完成标准：

- 能清楚解释一次 `输入文本 -> 返回 mp3 -> 播放音频 -> 嘴巴变化` 的完整链路

### 阶段 2：从“音量驱动”升级到“更像说话”

目标：

- 让嘴型不只是随音量大小变化，而是更稳定、更接近真实口型节奏

本阶段重点：

- 音频振幅、能量、频段的区别
- 嘴型平滑处理
- 开口/闭口阈值
- 简单 viseme / phoneme 映射的概念

建议任务：

- 给嘴型动画加平滑过渡，减少抖动
- 区分静音、轻声、重音
- 为嘴型状态设计 3 到 5 个离散档位

完成标准：

- 嘴型不再明显抖动
- 停顿、说话、结束状态更自然

### 阶段 3：从“静态头像”升级到“可用角色”

目标：

- 提升角色观感，让它更像一个基础数字人，而不是一张图加一个黑色椭圆

本阶段重点：

- 角色素材组织
- 五官分层绘制
- 眨眼、头部轻微摆动、呼吸感
- 2D 数字人渲染思路

建议任务：

- 将嘴巴、眼睛、底图拆层
- 增加眨眼动画
- 给角色增加待机状态

完成标准：

- 角色在不说话时也有基础生命感

### 阶段 4：接入“对话大脑”

目标：

- 从“输入一句文本让它读出来”，升级为“用户说/输入一句，角色自动回复”

本阶段重点：

- LLM 接入
- Prompt 设计
- 对话状态管理
- 文本输出和语音输出的衔接

建议任务：

- 增加 `/chat` 接口
- 将 LLM 输出交给 TTS 再播放
- 为角色定义基础人设

完成标准：

- 可以完成一轮“输入 -> 思考 -> 回复 -> 发声”

### 阶段 5：实时化与工程化

目标：

- 让系统从 Demo 走向更接近产品原型

本阶段重点：

- 流式输出
- 边生成边播放
- WebSocket
- 前后端状态同步
- 错误处理和日志

建议任务：

- 增加流式 TTS / 分段播放能力
- 增加会话状态与错误提示
- 整理项目目录结构

完成标准：

- 响应更快，结构更清晰，可持续迭代

### 阶段 6：进一步升级方向

按兴趣选择，不要求同时推进：

- 3D 数字人
- 实时语音对话
- ASR 语音识别
- 情绪表达
- 多角色切换
- RAG / 业务知识库
- 摄像头驱动表情
- Live2D / Unity / Unreal 集成

---

## 当前仓库的推进原则

后续在这个仓库里协作时，默认遵守以下规则：

1. 优先做“小步可运行”的改动，不直接引入过重框架。
2. 每次改动都要明确属于哪个学习阶段。
3. 优先解释“为什么这样做”，再实现代码。
4. 如果当前 Demo 还没吃透，不提前跳到复杂的实时流式架构。
5. 新增依赖前，先说明它解决的问题和替代方案。
6. 每完成一个阶段，补充文档和下一阶段任务。

---

## 建议的目录演进

随着学习推进，建议逐步整理为：

```text
digital_human/
├── Agents.md
├── main.py
├── static/
├── app/
│   ├── api/
│   ├── services/
│   ├── tts/
│   ├── chat/
│   └── avatar/
├── assets/
│   ├── avatar/
│   ├── audio/
│   └── motion/
└── docs/
    ├── stage-01.md
    ├── stage-02.md
    └── notes.md
```

当前项目还很小，暂时不必一次性调整到这个结构。

---

## 当前最值得做的下一步

如果没有额外说明，优先做下面 3 件事：

1. 给当前嘴型动画加平滑处理，减少抖动。
2. 给项目补一个 `README.md`，说明怎么运行当前 Demo。
3. 把“阶段 1”的学习笔记沉淀成文档。

---

## 给后续 Agent 的工作指令

如果后续继续在这个仓库中协作，默认按下面方式工作：

- 先判断当前需求属于哪个学习阶段。
- 优先基于现有代码增量修改，不推倒重来。
- 输出要面向“学习者可理解”，不要只追求功能完成。
- 如果任务跨度过大，先拆成 2 到 4 个可验证的小任务。
- 做完代码后，补一句“这一改动帮助理解了什么”。

---

## 当前阶段结论

这个仓库不是完整数字人产品，而是一个用于学习数字人核心链路的实验项目。

当前建议主线：

`先学清楚 TTS、音频播放、嘴型驱动，再进入角色表现和对话系统。`
